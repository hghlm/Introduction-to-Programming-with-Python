# my_list = [1, 2, 3, [4, 5, 6]]
# another_list = list(my_list)

print("1. The lists assigned are equal")
print("2. mylist and anotherlist are not the same object")
print("3. the nested lists at position 3 are equal")
print("4. the nested lists at position 3 are the same object")